{
  "name": "pr-kite-dashboard",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
